# JA_Stock 数据库表结构说明

> 本文档根据当前项目代码中的 SQLAlchemy 建表定义、运行时 `ALTER TABLE`、写库逻辑和查询逻辑整理。
> 代码未提供独立的数据库迁移文件，因此本文描述的是“按当前代码初始化后应有的结构”，不等同于某个线上数据库的实时结构。

## 1. 数据库概览

### 1.1 数据库类型

- 数据库：MySQL
- ORM/访问层：SQLAlchemy + PyMySQL
- 连接配置：`config/web_settings.json`（首次迁移时可回退到 `config/settings.local.py`）
- 数据库名、主机、端口、用户名和密码：读取 `DB_CONFIG`，项目代码不在本文档中展开真实配置
- 默认字符集：监控池表显式使用 `utf8mb4`；其他表由 MySQL/连接配置决定

### 1.2 表清单

| 表名 | 类型/用途 | 主要来源 |
|---|---|---|
| `daily_data` | 个股日线行情、估值、技术指标及评分宽表 | Tushare + 程序计算 + 个股基础信息 |
| `industry_daily` | 行业日度平均涨幅及滚动涨幅 | `daily_data` 聚合计算 |
| `concept_detail` | 个股与概念的多对多映射 | 同花顺问财 `pywencai` |
| `concept_daily` | 概念日度合成指数及滚动涨幅 | `daily_data` + `concept_detail` 计算 |
| `monitor_pool_stock` | 监控股票池 | Excel 或 Web 配置 |
| `monitor_pool_concept` | 监控概念池 | Excel 或 Web 配置 |
| `stock_financials` | 个股报告期财务快照 | AkShare 东方财富财报接口 |
| `daily_rankings` | 个股每日综合评分及排名 | `daily_data` + 财务数据 + 行业/概念数据计算 |
| `selection_history` | 策略每日选股结果及回测历史 | 策略引擎计算 |
| `index_daily` | 关键指数日线行情 | Tushare `index_daily` |

## 2. 表结构详情

### 2.1 `daily_data`：个股日线主表

代码位置：`core/data_updater.py` 的 `_ensure_table_structure()`。表名由 `DAILY_DATA_TABLE` 配置，当前值为 `daily_data`。

粒度：一只股票一个交易日一条记录。当前代码通过“先删除目标交易日再追加”的方式实现增量幂等；代码中未定义 `(ts_code, trade_date)` 唯一索引，建议生产库额外增加唯一约束前先清理历史重复数据。

| 字段名 | 类型 | 含义 | 来源 |
|---|---|---|---|
| `id` | INT，自增主键 | 记录主键 | 数据库生成 |
| `ts_code` | VARCHAR(20)，非空，索引 | Tushare 标准股票代码，如 `000001.SZ` | Tushare `daily`，并用于关联基础信息 |
| `trade_date` | VARCHAR(10)，非空，索引 | 交易日期，通常为 `YYYYMMDD` | Tushare `daily` |
| `open` | FLOAT | 开盘价 | Tushare `daily` |
| `high` | FLOAT | 最高价 | Tushare `daily` |
| `low` | FLOAT | 最低价 | Tushare `daily` |
| `close` | FLOAT | 收盘价 | Tushare `daily` |
| `pre_close` | FLOAT | 昨日收盘价 | Tushare `daily` |
| `change` | FLOAT | 涨跌额 | Tushare `daily` |
| `pct_chg` | FLOAT | 涨跌幅，百分比 | Tushare `daily` |
| `vol` | FLOAT | 成交量，Tushare 原始单位 | Tushare `daily` |
| `amount` | FLOAT | 成交额，Tushare 原始单位 | Tushare `daily` |
| `adj_factor` | FLOAT | 复权因子 | Tushare `adj_factor` |
| `adj_close` | FLOAT | 复权收盘价 | 代码预留字段；当前更新流程主要使用 `close * adj_factor` 计算指标 |
| `turnover_rate` | FLOAT | 换手率 | Tushare `daily_basic` |
| `turnover_rate_f` | FLOAT | 自由流通股换手率 | Tushare `daily_basic` |
| `volume_ratio` | FLOAT | 量比 | Tushare `daily_basic`；缺失时可用程序计算值补齐 |
| `pe` | FLOAT | 市盈率 | Tushare `daily_basic` |
| `pe_ttm` | FLOAT | TTM 市盈率 | Tushare `daily_basic` |
| `pb` | FLOAT | 市净率 | Tushare `daily_basic` |
| `total_mv` | FLOAT | 总市值，Tushare 原始单位 | Tushare `daily_basic` |
| `circ_mv` | FLOAT | 流通市值，Tushare 原始单位 | Tushare `daily_basic` |
| `ma_5` | FLOAT | 5 日收盘价均线 | `TechnicalCalculator` 计算 |
| `ma_10` | FLOAT | 10 日收盘价均线 | `TechnicalCalculator` 计算 |
| `ma_20` | FLOAT | 20 日收盘价均线 | `TechnicalCalculator` 计算 |
| `ma_30` | FLOAT | 30 日收盘价均线 | `TechnicalCalculator` 计算 |
| `ma_60` | FLOAT | 60 日收盘价均线 | `TechnicalCalculator` 计算 |
| `ma_120` | FLOAT | 120 日收盘价均线 | `TechnicalCalculator` 计算 |
| `ma_250` | FLOAT | 250 日收盘价均线 | `TechnicalCalculator` 计算 |
| `high_30` | FLOAT | 近 30 日最高价 | `TechnicalCalculator` 计算 |
| `high_60` | FLOAT | 近 60 日最高价 | `TechnicalCalculator` 计算 |
| `high_90` | FLOAT | 近 90 日最高价 | `TechnicalCalculator` 计算 |
| `high_120` | FLOAT | 近 120 日最高价 | `TechnicalCalculator` 计算 |
| `high_250` | FLOAT | 近 250 日最高价 | `TechnicalCalculator` 计算 |
| `pct_chg_1m` | FLOAT | 约 1 个月累计涨幅，按 20 个交易日计算 | `TechnicalCalculator` 计算 |
| `pct_chg_3m` | FLOAT | 约 3 个月累计涨幅，按 60 个交易日计算 | `TechnicalCalculator` 计算 |
| `pct_chg_6m` | FLOAT | 约 6 个月累计涨幅，按 120 个交易日计算 | `TechnicalCalculator` 计算 |
| `up_streak` | INT | 连续上涨天数 | `TechnicalCalculator` 计算 |
| `down_streak` | INT | 连续下跌天数 | `TechnicalCalculator` 计算 |
| `limit_up_count_10` | INT | 近 10 日涨停次数 | `TechnicalCalculator` 计算 |
| `limit_up_count_30` | INT | 近 30 日涨停次数 | `TechnicalCalculator` 计算 |
| `limit_up_count_90` | INT | 近 90 日涨停次数 | `TechnicalCalculator` 计算 |
| `limit_up_count_120` | INT | 近 120 日涨停次数 | `TechnicalCalculator` 计算 |
| `limit_up_count_180` | INT | 近 180 日涨停次数 | `TechnicalCalculator` 计算 |
| `stock_name` | VARCHAR(50) | 股票简称 | Tushare `stock_basic` |
| `industry` | VARCHAR(50) | 所属行业 | Tushare `stock_basic` |
| `area` | VARCHAR(50) | 所在地区 | Tushare `stock_basic` |
| `market` | VARCHAR(20) | 市场类型/板块标识 | Tushare `stock_basic` |
| `processed_time` | DATETIME | 本次处理或入库时间 | 程序生成 |
| `total_score` | FLOAT | 个股多因子综合评分 | `FactorCalculator` 计算并回写 |
| `concept_str` | VARCHAR(255) | 个股概念名称拼接字符串 | `concept_detail` 聚合后由 `FactorCalculator` 回写 |

说明：`total_score` 和 `concept_str` 不是最初 `Table(...)` 定义中的字段，而是由 `core/factor_calculator.py` 启动时通过 `ALTER TABLE` 自动补充。

### 2.2 `industry_daily`：行业日线表

粒度：一个行业一个交易日一条记录。

| 字段名 | 类型 | 含义 | 来源 |
|---|---|---|---|
| `id` | INT，自增主键 | 记录主键 | 数据库生成 |
| `trade_date` | VARCHAR(10)，非空，索引 | 交易日期 | `daily_data.trade_date` |
| `industry_name` | VARCHAR(50)，非空，索引 | 行业名称 | `daily_data.industry` |
| `avg_pct` | FLOAT | 行业内股票当日平均涨跌幅 | 对 `daily_data.pct_chg` 分行业平均 |
| `pct_5d` | FLOAT | 行业 5 日累计涨幅 | 程序滚动复合计算 |
| `pct_10d` | FLOAT | 行业 10 日累计涨幅 | 程序滚动复合计算 |
| `pct_20d` | FLOAT | 行业 20 日累计涨幅 | 程序滚动复合计算 |
| `processed_time` | DATETIME | 处理时间 | 程序生成 |

计算时会排除 ST 股票以及部分北交所/特殊代码股票，具体条件见 `update_industry_metrics()`。

### 2.3 `concept_detail`：个股-概念映射表

粒度：一只股票与一个概念一条映射记录；一只股票可对应多个概念。

| 字段名 | 类型 | 含义 | 来源 |
|---|---|---|---|
| `id` | INT，自增主键 | 记录主键 | 数据库生成 |
| `ts_code` | VARCHAR(20)，非空，索引 | 标准股票代码 | 问财返回的股票代码，经程序标准化 |
| `stock_name` | VARCHAR(50) | 股票简称 | 问财“股票简称/名称”字段 |
| `concept_name` | VARCHAR(100)，非空，索引 | 概念名称 | 同花顺问财“所属概念”字段拆分 |
| `src` | VARCHAR(20)，默认 `wencai` | 映射来源标识 | 程序固定写入 `wencai` |
| `processed_time` | DATETIME | 处理时间 | 程序生成 |

问财数据通过 `pywencai.get(query='A股股票的所属概念', loop=True)` 获取；更新时按股票删除旧映射，再写入本次成功获取的映射。

### 2.4 `concept_daily`：概念日线表

粒度：一个概念一个交易日一条记录。该表是由个股涨跌幅和概念映射合成的“概念指数”，不是接口直接返回的指数行情。

| 字段名 | 类型 | 含义 | 来源 |
|---|---|---|---|
| `id` | INT，自增主键 | 记录主键 | 数据库生成 |
| `trade_date` | VARCHAR(10)，非空，索引 | 交易日期 | `daily_data.trade_date` |
| `concept_name` | VARCHAR(100)，非空，索引 | 概念名称 | `concept_detail.concept_name` |
| `avg_pct` | FLOAT | 概念内股票当日平均涨跌幅 | `daily_data.pct_chg` 按概念聚合 |
| `pct_5d` | FLOAT | 概念 5 日累计涨幅 | 程序滚动复合计算 |
| `pct_10d` | FLOAT | 概念 10 日累计涨幅 | 程序滚动复合计算 |
| `pct_20d` | FLOAT | 概念 20 日累计涨幅 | 程序滚动复合计算 |
| `pct_30d` | FLOAT | 概念 30 日累计涨幅 | 程序滚动复合计算 |
| `pct_60d` | FLOAT | 概念 60 日累计涨幅 | 程序滚动复合计算 |
| `pct_90d` | FLOAT | 概念 90 日累计涨幅 | 程序滚动复合计算 |
| `pct_120d` | FLOAT | 概念 120 日累计涨幅 | 程序滚动复合计算 |
| `pct_250d` | FLOAT | 概念 250 日累计涨幅 | 程序滚动复合计算 |
| `processed_time` | DATETIME | 处理时间 | 程序生成 |

### 2.5 `monitor_pool_stock`：监控股票池

主键：`ts_code`。数据同步前会清空整张表，再从 Excel 或 Web 配置重建。

| 字段名 | 类型 | 含义 | 来源 |
|---|---|---|---|
| `ts_code` | VARCHAR(20)，主键 | 标准股票代码 | Excel“股票代码”或 Web 配置 `code`，程序标准化 |
| `name` | VARCHAR(50) | 股票名称 | Excel“股票名称”或 Web 配置 `name` |
| `remark` | TEXT | 用户备注 | Excel“备注”或 Web 配置 `remark` |
| `sort_order` | INT，默认 0 | 监控池展示顺序 | 按导入顺序由程序生成 |
| `update_time` | DATETIME | 同步时间 | 程序生成 |

### 2.6 `monitor_pool_concept`：监控概念池

主键：`concept_name`。数据同步前会清空整张表，再从 Excel 或 Web 配置重建。

| 字段名 | 类型 | 含义 | 来源 |
|---|---|---|---|
| `concept_name` | VARCHAR(50)，主键 | 要监控的概念名称 | Excel“概念名称”或 Web 配置 `name` |
| `sort_order` | INT，默认 0 | 监控池展示顺序 | 按导入顺序由程序生成 |
| `update_time` | DATETIME | 同步时间 | 程序生成 |

### 2.7 `stock_financials`：个股财务快照表

粒度：一只股票一个报告期一条记录。唯一索引：`(ts_code, period)`。

| 字段名 | 类型 | 含义 | 来源 |
|---|---|---|---|
| `id` | INT，自增主键 | 记录主键 | 数据库生成 |
| `ts_code` | VARCHAR(20)，索引 | 标准股票代码 | AkShare 返回代码，经程序补充交易所后缀 |
| `period` | VARCHAR(20)，索引 | 财务报告期，如 `20240930` | 根据目标交易日推算 |
| `ann_date` | VARCHAR(20) | 公告日期 | AkShare 东方财富财报接口 |
| `revenue` | FLOAT | 营业总收入，元 | AkShare `stock_yjbb_em` |
| `net_profit` | FLOAT | 净利润，元 | AkShare `stock_yjbb_em` |
| `roe` | FLOAT | 净资产收益率，百分比 | AkShare `stock_yjbb_em` |
| `processed_time` | VARCHAR(50) | 处理时间 | 程序生成 |

当前代码按目标日期自动选择可用报告期，并在成功获取非空数据后按报告期覆盖写入。

### 2.8 `daily_rankings`：每日综合排名表

粒度：一只股票一个交易日一条记录。唯一索引：`(trade_date, ts_code)`。

| 字段名 | 类型 | 含义 | 来源 |
|---|---|---|---|
| `id` | INT，自增主键 | 记录主键 | 数据库生成 |
| `trade_date` | VARCHAR(20)，索引 | 交易日期 | 计算任务参数 |
| `ts_code` | VARCHAR(20)，索引 | 标准股票代码 | `daily_data` |
| `stock_name` | VARCHAR(50) | 股票简称 | `daily_data.stock_name` |
| `industry` | VARCHAR(50) | 所属行业 | `daily_data.industry` |
| `total_mv` | FLOAT | 总市值；排名表注释定义为亿元 | `daily_data.total_mv` 转换后保存 |
| `total_score` | FLOAT | 多因子综合评分，代码注释标为 0~10 分 | 因子计算 |
| `ind_mv_rank` | INT | 股票在所属行业内的市值排名 | 程序排名计算 |
| `ind_stock_count` | INT | 所属行业股票总数 | 程序统计 |
| `ind_mv_ratio` | FLOAT | 行业龙头系数，代码注释定义为 0~1 | 程序计算 |
| `overall_rank` | INT | 全市场综合排名 | 程序排名计算 |
| `processed_time` | VARCHAR(50) | 处理时间 | 程序生成 |

### 2.9 `selection_history`：策略选股历史表

粒度：一只股票在一个交易日被一个策略选中一条记录。写入前会删除目标交易日数据；写入 DataFrame 时会按 `(trade_date, ts_code, strategy_name)` 去重。

| 字段名 | 类型 | 含义 | 来源 |
|---|---|---|---|
| `id` | INT，自增主键 | 记录主键 | 数据库生成 |
| `trade_date` | VARCHAR(20)，索引 | 选股交易日 | 策略运行日期 |
| `ts_code` | VARCHAR(20)，索引 | 标准股票代码 | `daily_data` |
| `stock_name` | VARCHAR(50) | 股票简称 | `daily_data` |
| `strategy_name` | VARCHAR(50) | 命中的策略名称 | 策略引擎，目前包括策略 1~5 |
| `initial_price` | FLOAT | 入选时初始价格 | `daily_data.close`，写库时映射为 `initial_price` |
| `total_score` | FLOAT | 入选时综合评分 | `daily_data`/因子计算结果 |
| `total_mv` | FLOAT | 入选时总市值 | `daily_data.total_mv`，展示层可能转换单位 |
| `industry` | VARCHAR(50) | 所属行业 | `daily_data.industry` |
| `market` | VARCHAR(20) | 市场/板块 | `daily_data.market` 或市场推断逻辑 |
| `processed_time` | VARCHAR(50) | 入库时间 | 程序生成 |

### 2.10 `index_daily`：关键指数日线表

粒度：一个指数一个交易日一条记录。唯一索引：`(trade_date, ts_code)`。
每日复盘调用 `FactorCalculator.get_key_index_panel()` 获取并覆盖写入目标交易日数据。
当前保存的指数包括上证指数、上证50、沪深300、中证500、中证1000、科创50和创业板指。

| 字段名 | 类型 | 含义 | 来源 |
|---|---|---|---|
| `id` | INT，自增主键 | 记录主键 | 数据库生成 |
| `trade_date` | VARCHAR(20)，非空，索引 | 交易日期 | Tushare `index_daily` 查询日期 |
| `ts_code` | VARCHAR(20)，非空，索引 | 指数代码，如 `000001.SH` | Tushare 指数代码 |
| `index_name` | VARCHAR(50)，非空 | 指数名称 | 程序配置映射 |
| `open` | FLOAT | 当日开盘点数 | Tushare `index_daily.open` |
| `close` | FLOAT | 当日收盘点数 | Tushare `index_daily.close` |
| `change` | FLOAT | 当日涨跌点数 | Tushare `index_daily.change` |
| `pct_chg` | FLOAT | 当日涨跌幅百分比 | Tushare `index_daily.pct_chg` |
| `processed_time` | VARCHAR(50) | 写入时间 | 程序生成 |

## 3. 数据来源与处理链路

```text
Tushare daily + daily_basic + adj_factor
                 │
                 ▼
           daily_data
          ┌──────┴─────────┐
          ▼                ▼
   industry_daily     技术指标/因子/排名
          │                │
          │          daily_rankings
          │
问财概念映射 ──► concept_detail ──► concept_daily

Tushare 指数日线 ──► index_daily

Excel/Web 监控配置 ──► monitor_pool_stock
                    └─► monitor_pool_concept

AkShare 财务接口 ──► stock_financials

策略计算 ──► selection_history
```

补充：`stock_basic` 和 `industry_detail` 目前只是配置常量/历史命名残留，当前代码没有对应的本地建表逻辑，不应视为本项目当前实际维护的数据库表。股票基础信息由 Tushare `stock_basic` 接口直接获取并在内存中使用。

## 4. 维护与使用建议

1. 查询行情主表时，通常使用 `(trade_date, ts_code)` 作为业务键；当前代码只对两个字段分别建索引，未统一定义唯一约束。
2. `daily_data` 中的均线、滚动涨幅、连涨连跌和涨停统计均为程序计算字段，重新导入原始行情后需要重新执行历史修复/指标计算。
3. `industry_daily` 和 `concept_daily` 是派生表，不能作为原始行情的唯一事实来源；如需重算，应先确认对应的 `daily_data` 和 `concept_detail` 完整。
4. `monitor_pool_stock`、`monitor_pool_concept` 是配置型表，同步操作会清空并重建内容，人工直接修改数据库可能被下一次同步覆盖。
5. `stock_financials` 的 `period` 是报告期，不是交易日期；回测或历史分析时应避免使用目标日期之后才披露的财务数据。
6. `index_daily` 在命令行的 `update`、`factor`、`review` 模式，以及 Web 控制台的“数据更新/全流程”中会自动初始化并更新；同一交易日会先删除旧记录，再写入最新获取的数据。
7. 日常完整更新命令为：`python main.py --mode update`。仅执行 `review` 只会生成报告，不会更新个股日线行情。
8. 本文档没有包含数据库密码、Token 等敏感配置；实际连接信息请查看本地 `config/web_settings.json` 或受保护的本地配置文件。

## 5. 代码依据

- `config/__init__.py`：数据库配置入口和表名常量
- `core/db_engine.py`：数据库连接及基础查询
- `core/data_updater.py`：`daily_data`、`industry_daily`、`concept_detail`、`concept_daily`
- `core/monitor_manager.py`：监控池表及同步逻辑
- `core/factor_calculator.py`：`stock_financials`、`daily_rankings`、`index_daily` 及 `daily_data` 动态字段
- `core/strategies.py`：`selection_history` 及策略结果写入
